# Copy to .env (sibling of this file) and fill in real values.
# `dotenv` loads this from process.cwd(), so run playwright from the repo root.

VITE_API_BASE_URL=
VITE_API_X_API_KEY=
TEST_USERNAME=
TEST_PASSWORD=
